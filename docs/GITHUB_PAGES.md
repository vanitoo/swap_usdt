# GitHub Pages

Проект уже настроен на `output: "export"` и workflow `.github/workflows/pages.yml`.

Для публикации выбери:

```text
Settings → Pages → Source: GitHub Actions
```

Для github.io используется basePath `/swap_usdt`.
